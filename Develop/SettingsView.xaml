<Page
    x:Class="Develop.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Develop"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    NavigationCacheMode="Required"
    mc:Ignorable="d">

    <muxc:TwoPaneView
        MinTallModeHeight="0"
        Pane1Length="360"
        PanePriority="Pane2"
        TallModeConfiguration="SinglePane"
        WideModeConfiguration="LeftRight">
        <muxc:TwoPaneView.Pane1>
            <Grid Background="{ThemeResource SystemChromeMediumLowColor}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <ListView
                    Grid.Row="1"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    SelectedIndex="{x:Bind SelectedPage, Mode=TwoWay}">
                    <ListViewItem Content="{x:Bind GeneralHeaderId, Mode=OneTime, Converter={StaticResource ResourceLocator}}" />
                    <ListViewItem Content="{x:Bind EditorOptionsHeaderId, Mode=OneTime, Converter={StaticResource ResourceLocator}}" />
                </ListView>
            </Grid>
        </muxc:TwoPaneView.Pane1>
        <muxc:TwoPaneView.Pane2>
            <FlipView
                x:Uid="SettingsHub"
                CornerRadius="0"
                SelectedIndex="{x:Bind SelectedPage, Mode=TwoWay}">
                <FlipView.ItemTemplate>
                    <DataTemplate x:DataType="StackPanel">
                        <ScrollViewer Padding="0,0,0,16" Background="{ThemeResource MaterialPageBackground}">
                            <ContentPresenter Margin="48" Content="{x:Bind}" />
                        </ScrollViewer>
                    </DataTemplate>
                </FlipView.ItemTemplate>
                <FlipView.Resources>
                    <Style TargetType="StackPanel">
                        <Setter Property="Spacing" Value="8" />
                    </Style>
                </FlipView.Resources>
                <StackPanel AutomationProperties.Name="{x:Bind GeneralHeaderId, Mode=OneWay, Converter={StaticResource ResourceLocator}}">
                    <TextBlock x:Uid="Settings_General" Style="{ThemeResource SubheaderTextBlockStyle}" />
                    <ToggleSwitch x:Uid="SettingsDisableSound" IsOn="{x:Bind local:SettingsViewModel.Instance.DisableSound, Mode=TwoWay}" />
                </StackPanel>
                <StackPanel AutomationProperties.Name="{x:Bind EditorOptionsHeaderId, Mode=OneWay, Converter={StaticResource ResourceLocator}}">
                    <TextBlock x:Uid="Settings_Editor" Style="{ThemeResource SubheaderTextBlockStyle}" />
                    <ToggleSwitch x:Uid="EditorSettingsAutoSave" IsOn="{x:Bind local:SettingsViewModel.Instance.AutoSave, Mode=TwoWay}" />
                    <HyperlinkButton x:Uid="EditorSettingsManageFontsList" NavigateUri="ms-settings:fonts" />
                </StackPanel>
                <FlipView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel Orientation="Vertical" />
                    </ItemsPanelTemplate>
                </FlipView.ItemsPanel>
            </FlipView>
            <!--
            <Hub x:Uid="SettingsHub">
                <HubSection x:Uid="Settings_Editor" DataContext="{x:Bind local:AppSettings.Preferences}">
                    <DataTemplate x:DataType="managed:SettingsViewModel">
                        <StackPanel Orientation="Vertical" Spacing="8">
                            <ToggleSwitch x:Uid="EditorSettingsAutoSave" IsOn="{x:Bind AutoSave, Mode=TwoWay}" />
                            <HyperlinkButton x:Uid="EditorSettingsManageFontsList" NavigateUri="ms-settings:fonts" />
                            <muxc:NumberBox
                                x:Uid="FontSizeOption"
                                AcceptsExpression="False"
                                Maximum="72"
                                Minimum="12"
                                SmallChange="1"
                                SpinButtonPlacementMode="Inline"
                                ValidationMode="InvalidInputOverwritten"
                                Value="{x:Bind FontSize, Mode=TwoWay}" />
                        </StackPanel>
                    </DataTemplate>
                </HubSection>
            </Hub>-->
        </muxc:TwoPaneView.Pane2>
    </muxc:TwoPaneView>
</Page>
