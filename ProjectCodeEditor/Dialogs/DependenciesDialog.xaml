<ContentDialog
    x:Class="ProjectCodeEditor.Dialogs.DependenciesDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:ProjectCodeEditor.Dialogs"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:root="using:ProjectCodeEditor"
    xmlns:vmodels="using:ProjectCodeEditor.ViewModels"
    Title="{x:Bind local:DependenciesDialog.DependenciesBlockTextId, Converter={StaticResource ResourceLocator}}"
    CloseButtonText="{x:Bind OkayStringId, Converter={StaticResource ResourceLocator}}"
    DefaultButton="Close"
    mc:Ignorable="d">

    <ItemsControl
        x:Uid="DependenciesList"
        IsTabStop="False"
        ItemsSource="{x:Bind root:App.AppSettings.AppDependencies, Mode=OneTime}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate x:DataType="vmodels:Dependency">
                <HyperlinkButton Content="{x:Bind DependencyName}" NavigateUri="{x:Bind ProjectUri}" />
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</ContentDialog>
